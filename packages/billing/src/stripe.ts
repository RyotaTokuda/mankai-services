/**
 * Stripe クライアント初期化（サーバーサイド専用）
 *
 * TODO: Phase 4 で実装
 * - npm install stripe
 * - STRIPE_SECRET_KEY を環境変数に追加
 */

// export function getStripe() {
//   const Stripe = require("stripe");
//   return new Stripe(process.env.STRIPE_SECRET_KEY!, {
//     apiVersion: "2024-12-18.acacia",
//   });
// }
